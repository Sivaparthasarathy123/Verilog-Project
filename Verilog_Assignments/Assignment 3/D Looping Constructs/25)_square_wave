//Forever loop to generate a square wave

module forever_square_wave;
  reg clk;

  initial begin
    clk = 0;
    forever #5 clk = ~clk; 
  end

  initial begin
    $monitor("Time=%0t | clk=%b", $time, clk);
    #50 $finish;
  end
endmodule

