 task and_or_operation;
        input a, b;       
        output y, z;  
    begin
        y = a & b;   
        z  = a | b;   
        $display("Inputs: a=%b, b=%b | AND=%b, OR=%b", a , b, y, z);
    end
    endtask